{
  "description": "Search the internet with brave search",
  "parameters": {
    "type": "object",
    "properties": {
      "text": {
        "type": "string",
        "description": "Query to search"
      }
    },
    "required": ["text"]
  }
}
